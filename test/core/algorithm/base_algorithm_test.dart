import 'package:flutter_test/flutter_test.dart';
import 'package:scheduling_algorithm/core/algorithm/schedule.dart';
import 'package:scheduling_algorithm/core/model/task/result/task_result.dart';
import 'package:scheduling_algorithm/core/model/task/task.dart';

// class MockTask extends Task {}

// class MockScheduling extends Schedule {
//   MockScheduling(List<Task> tasks) : super(tasks);

//   @override
//   List<TaskResult> execute() => [];
// }

void main() {
  // List<Task> generateTaskList() => [MockTask(), MockTask(), MockTask()];
  // Schedule generateScheduling() => MockScheduling(generateTaskList());
  // group('Pop functions', () {
  //   test("Must pop a task", () {
  //     Schedule scheduling = generateScheduling();
  //     int size = scheduling.taskQueue.length;
  //     Task? popEnd = scheduling.popEnd();
  //     expect(popEnd != null, true);
  //     expect(scheduling.taskQueue.length, size - 1);
  //   });
  // });
}
